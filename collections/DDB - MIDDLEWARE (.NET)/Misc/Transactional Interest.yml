info:
  name: Transactional Interest
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url_dev}}/api/digiagri/transactional/interest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
