info:
  name: sikp verify
  type: http
  seq: 4

http:
  method: GET
  url: "{{dev_new}}/api/digiagri/sikpverify/{{userId}}/{{noKTP}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: success
    request:
      url: "{{base_url}}/api/digiagri/sikpverify/{{userId}}/{{noKTP}}"
      method: GET
    response:
      body:
        type: text
        data: "{\r

          \    \"statusCode\": \"1\",\r

          \    \"statusDescription\": \"Inquiry Success, Calon debitur belum terdaftar di SIKP, pengajuan bisa dilakukan\"\r

          }"
