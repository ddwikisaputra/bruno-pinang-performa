info:
  name: "[Prescreening] sikpverify"
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/digiagri/sikpverify/{{userId}}/{{NIK}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
