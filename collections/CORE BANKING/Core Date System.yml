info:
  name: Core Date System
  type: http
  seq: 29

http:
  method: GET
  url: "{{URL}}/system/date"
  auth:
    type: basic
    username: pinang-mikro
    password: "0"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
