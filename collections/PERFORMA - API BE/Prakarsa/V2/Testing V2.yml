info:
  name: Testing V2
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/v/2/prakarsa/testing"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
