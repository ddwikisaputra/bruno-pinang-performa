info:
  name: "[PRODUCT] List"
  type: http
  seq: 13

http:
  method: GET
  url: "{{url}}/bem/v1/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
