info:
  name: REFRESH TOKEN
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/refresh-token"
  body:
    type: form-urlencoded
  auth:
    type: bearer
    token: "{{tokenJWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
