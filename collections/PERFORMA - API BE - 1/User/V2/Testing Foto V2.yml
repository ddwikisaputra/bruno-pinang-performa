info:
  name: Testing Foto V2
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v/2/user/testing-foto"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
