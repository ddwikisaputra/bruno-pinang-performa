info:
  name: Postal Codes V1
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/v/1/master/postal-codes/3171051002"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: success
    request:
      url: "{{base_url}}/v/1/master/postal-codes/{{villageId}}"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "data": [
                  {
                      "postalCode": "24261",
                      "postalCodeName": "24261"
                  }
              ],
              "desc": "Data Valid",
              "message": "Data ditemukan",
              "status": 200
          }
  - name: unautorized
    request:
      url: "{{base_url}}/v/1/master/postal-codes/{{villageId}}"
      method: GET
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: |-
          {
              "code": 401,
              "message": "signature is invalid || Token is expired"
          }
  - name: validation param
    request:
      url: "{{base_url}}/v/1/master/postal-codes/{{villageId}}"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "status": 500,
              "message": "Maaf, parameter kelurahan belum dipilih",
              "desc": "Parameter villageId belum terisi",
              "data" : null
          }
  - name: not found
    request:
      url: "{{base_url}}/v/1/master/postal-codes/{{villageId}}"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "status": 404,
              "message": "Kode Pos Tidak Ditemukan",
              "desc": "Data Kode Pos Tidak Ditemukan",
              "data": null
          }
  - name: general error
    request:
      url: "{{base_url}}/v/1/master/postal-codes/{{villageId}}"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "status": 500,
              "message": "Server sedang maintenance, silahkan coba beberapa saat lagi",
              "desc": "Could not connect .....",
              "data" : null
          }

docs: |-
  'province' diisi dari parameter 'code' yang didapat dari endpoint 'master-province'

  'kota-kabupaten' diisi dari parameter 'dati2Code' yang didapat dari endpoint 'master-kota-kabupaten'

  'kecamatan' diisi dari parameter 'kecamatanCode' yang didapat dari endpoint 'master-kecamatan'
